# 🔧 حل مشكلة Docker في Railway

## المشكلة:
Railway يحاول استخدام Docker بدلاً من NIXPACKS، وهذا يسبب خطأ "client directory not found"

## ✅ الحل السريع:

### الخطوة 1: تأكد من رفع التغييرات إلى GitHub

```bash
git add .
git commit -m "Remove Dockerfile, use NIXPACKS"
git push
```

### الخطوة 2: غير Builder في Railway

1. اذهب إلى مشروعك على Railway
2. اضغط على **"Settings"** (الإعدادات)
3. ابحث عن قسم **"Build"** أو **"Builder"**
4. غيّر من **"Docker"** إلى **"NIXPACKS"**
5. احفظ التغييرات

### الخطوة 3: احذف الـ Deployment القديم

1. اذهب إلى **"Deployments"**
2. احذف الـ deployment الفاشل
3. أو اضغط **"Redeploy"** بعد تغيير Builder

### الخطوة 4: تحقق من railway.json

تأكد أن ملف `railway.json` موجود ويحتوي على:

```json
{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run install-all && npm run build"
  }
}
```

---

## 🚨 إذا لم تجد خيار Builder في Settings:

### الطريقة البديلة:

1. **احذف المشروع الحالي** من Railway
2. **أنشئ مشروع جديد**
3. **اربطه مع نفس الـ repository**
4. Railway سيكتشف `railway.json` تلقائياً ويستخدم NIXPACKS

---

## ✅ تأكد من:

- [ ] لا يوجد `Dockerfile` في المشروع
- [ ] ملف `railway.json` موجود
- [ ] `railway.json` يحتوي على `"builder": "NIXPACKS"`
- [ ] رفعت التغييرات إلى GitHub
- [ ] Builder في Railway = NIXPACKS (ليس Docker)

---

## 📝 ملاحظة:

إذا كان المشروع القديم يعمل، ربما كان يستخدم إعدادات مختلفة. 
المشروع الجديد يحتاج أن يكون Builder = NIXPACKS.



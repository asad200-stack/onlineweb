# 🚀 دليل النشر السريع على Railway

## خطوات النشر (3 دقائق)

### 1️⃣ ارفع الكود على GitHub
```bash
git add .
git commit -m "Ready for Railway"
git push
```

### 2️⃣ أنشئ مشروع جديد على Railway

1. اذهب إلى [railway.app](https://railway.app)
2. اضغط **"New Project"**
3. اختر **"Deploy from GitHub repo"**
4. اختر الـ repository الخاص بك

### 3️⃣ أضف متغير البيئة (مهم جداً!)

في صفحة المشروع:
- اضغط **"Variables"**
- اضغط **"New Variable"**
- أضف:
  ```
  NODE_ENV = production
  ```

### 4️⃣ انتظر البناء

- Railway سيبني المشروع تلقائياً
- انتظر حتى يظهر "Deployed successfully" ✅

### 5️⃣ احصل على الرابط

- اضغط **"Settings"**
- في قسم **"Domains"** اضغط **"Generate Domain"**
- أو أضف domain مخصص

---

## ✅ تأكد من:

- [ ] ملف `railway.json` موجود
- [ ] ملف `package.json` موجود في الجذر
- [ ] ملف `package.json` موجود في `client/`
- [ ] **لا يوجد** `Dockerfile` (سيسبب مشاكل!)
- [ ] `NODE_ENV=production` موجود في Variables

---

## 🔑 معلومات الدخول

- **اسم المستخدم:** `web`
- **كلمة المرور:** `web12345`

⚠️ غيّر كلمة المرور بعد أول دخول!

---

## 🐛 إذا فشل البناء:

1. تحقق من **Logs** في Railway
2. تأكد من `NODE_ENV=production` في Variables
3. تأكد أن لا يوجد `Dockerfile`
4. راجع ملف `RAILWAY_SETUP.md` للحلول التفصيلية

---

## 📞 ملاحظات

- `PORT` يتم إضافته تلقائياً من Railway - لا تضيفه
- قاعدة البيانات تُنشأ تلقائياً
- الصور تُحفظ في `server/uploads`



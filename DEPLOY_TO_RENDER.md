# دليل النشر على Render.com

## 📋 المتطلبات
- حساب على Render.com (مجاني): https://render.com
- حساب GitHub (لربط المشروع)

---

## 🚀 خطوات النشر

### 1. إعداد المشروع على GitHub

1. أنشئ repository جديد على GitHub
2. ارفع المشروع:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin YOUR_GITHUB_REPO_URL
git push -u origin main
```

### 2. إنشاء حساب على Render

1. اذهب إلى: https://render.com
2. سجل دخول بحساب GitHub
3. اربط حساب GitHub

### 3. نشر المشروع

#### الطريقة 1: استخدام ملف render.yaml (الأسهل)

1. في Render Dashboard، اضغط "New +"
2. اختر "Blueprint"
3. اربط repository الخاص بك
4. Render سيكتشف ملف `render.yaml` تلقائياً
5. اضغط "Apply"

#### الطريقة 2: النشر اليدوي

1. في Render Dashboard، اضغط "New +"
2. اختر "Web Service"
3. اربط repository الخاص بك
4. املأ البيانات:
   - **Name:** ecommerce-store
   - **Environment:** Node
   - **Build Command:** `npm run install-all && npm run build`
   - **Start Command:** `npm start`
   - **Plan:** Free

### 4. إعداد متغيرات البيئة (Environment Variables)

في صفحة المشروع على Render، اذهب إلى "Environment":

```
NODE_ENV=production
PORT=10000
JWT_SECRET=your-super-secret-random-string-here
```

**مهم:** غير `JWT_SECRET` إلى نص عشوائي قوي!

### 5. إعداد القرص (Disk) للبيانات

1. في Render Dashboard، اضغط "New +"
2. اختر "Persistent Disk"
3. املأ البيانات:
   - **Name:** ecommerce-disk
   - **Mount Path:** `/opt/render/project/src/server`
   - **Size:** 1GB (مجاني)
4. اربطه بالخدمة (Service) الخاص بك

---

## 🔧 بعد النشر

### 1. الحصول على الرابط

بعد النشر، Render سيعطيك رابط مثل:
```
https://ecommerce-store.onrender.com
```

### 2. إعداد رابط المتجر في الإعدادات

1. سجل دخول للوحة التحكم: `https://YOUR_APP.onrender.com/admin/login`
2. اذهب إلى الإعدادات
3. في حقل "رابط المتجر العام"، أدخل: `https://YOUR_APP.onrender.com`
4. احفظ الإعدادات

### 3. تغيير كلمة المرور الافتراضية

⚠️ **مهم جداً:** غير كلمة المرور الافتراضية فوراً!

1. سجل دخول بحساب admin
2. يمكنك تغيير كلمة المرور من قاعدة البيانات أو إنشاء حساب جديد

---

## 📝 ملاحظات مهمة

### 1. الخطة المجانية:
- الخدمة تدخل في "sleep mode" بعد 15 دقيقة من عدم الاستخدام
- أول طلب بعد الاستيقاظ قد يأخذ 30-50 ثانية
- للاستخدام الجدي، احتاج خطة مدفوعة

### 2. قاعدة البيانات:
- SQLite يعمل على القرص المستمر (Persistent Disk)
- للاستخدام الكبير، يُفضل الانتقال لـ PostgreSQL (Render يدعمه)

### 3. الملفات المرفوعة:
- الصور تُحفظ في `/server/uploads`
- تأكد من إعداد Persistent Disk لحفظ الملفات

---

## 🎯 بدائل للنشر

### 1. Railway.app (سريع جداً)
- رابط: https://railway.app
- مجاني لمدة شهر، ثم حسب الاستخدام
- أسرع من Render في الاستيقاظ

### 2. Vercel (Frontend) + Railway/Render (Backend)
- Frontend على Vercel (مجاني وسريع)
- Backend على Railway أو Render
- يحتاج تعديل في الإعدادات

### 3. DigitalOcean / AWS
- احترافي وموثوق
- مدفوع لكن أرخص للاستخدام الكبير

---

## 🐛 حل المشاكل الشائعة

### المشكلة: الخدمة لا تعمل
- تحقق من الـ Logs في Render
- تأكد من أن Build Command نجح
- تحقق من Start Command

### المشكلة: قاعدة البيانات فارغة
- تأكد من إعداد Persistent Disk
- تحقق من مسار قاعدة البيانات

### المشكلة: الصور لا تظهر
- تحقق من مسار `/uploads`
- تأكد من إعداد Static Files في Express

---

## ✅ بعد النشر الناجح

1. ✅ اربط الرابط في الإعدادات
2. ✅ غير كلمة المرور
3. ✅ اختبر إضافة منتج
4. ✅ اختبر مشاركة الرابط

**بالتوفيق! 🚀**


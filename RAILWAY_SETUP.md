# دليل إعداد Railway - خطوة بخطوة

## ✅ قائمة التحقق قبل النشر

- [ ] المشروع موجود على GitHub
- [ ] ملف `railway.json` موجود
- [ ] ملف `package.json` موجود في الجذر
- [ ] ملف `package.json` موجود في `client/`
- [ ] لا يوجد `Dockerfile` (سيسبب مشاكل)

## 🔧 إعدادات Railway المطلوبة

### 1. إنشاء مشروع جديد

1. اذهب إلى [railway.app](https://railway.app)
2. اضغط "New Project"
3. اختر "Deploy from GitHub repo"
4. اختر الـ repository

### 2. إعدادات البناء (Build Settings) ⚠️ مهم جداً!

في صفحة المشروع → **Settings**:

- **Builder:** **يجب أن يكون `NIXPACKS`** (إذا كان `Docker`، غيّره!)
- **Build Command:** اتركه فارغاً (سيستخدم من `railway.json`)
- **Start Command:** اتركه فارغاً (سيستخدم من `railway.json`)

**🚨 إذا كان Builder = Docker:**
- غيّره إلى `NIXPACKS` فوراً
- أو احذف المشروع وأنشئ مشروع جديد

### 3. متغيرات البيئة (Environment Variables)

في صفحة المشروع → **Variables** → اضغط "New Variable":

```
NODE_ENV = production
```

⚠️ **مهم:** 
- `PORT` يتم إضافته تلقائياً من Railway - لا تضيفه يدوياً
- لا تضيف `JWT_SECRET` إلا إذا كان الكود يحتاجه

### 4. التحقق من الإعدادات

تأكد أن `railway.json` يحتوي على:

```json
{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run install-all && npm run build"
  },
  "deploy": {
    "startCommand": "npm start"
  }
}
```

## 🚨 حل المشاكل الشائعة

### المشكلة: Build Failed - "can't cd to client"

**الحل:**
- تأكد أن مجلد `client/` موجود في المشروع
- تأكد أن `railway.json` موجود
- تأكد أن لا يوجد `Dockerfile` في المشروع

### المشكلة: Build Failed - "npm run install-all"

**الحل:**
- تحقق من Logs في Railway
- تأكد أن `package.json` موجود في الجذر
- تأكد أن `client/package.json` موجود

### المشكلة: السيرفر لا يبدأ

**الحل:**
- تأكد من `NODE_ENV=production` في Variables
- تحقق من Logs لرؤية الخطأ
- تأكد أن `PORT` موجود (Railway يضيفه تلقائياً)

### المشكلة: الصفحة تظهر "Cannot GET /"

**الحل:**
- تأكد أن `NODE_ENV=production` موجود
- تأكد أن البناء اكتمل بنجاح
- تحقق من أن `client/dist` تم إنشاؤه

## 📊 مراقبة النشر

1. **Deployments Tab:** شاهد حالة البناء
2. **Logs Tab:** شاهد السجلات في الوقت الفعلي
3. **Metrics Tab:** شاهد استخدام الموارد

## 🔄 إعادة النشر

إذا أردت إعادة النشر:

1. ادفع تغييرات جديدة إلى GitHub
2. Railway سيكتشف التغييرات تلقائياً
3. سيبدأ بناء جديد تلقائياً

أو:

1. اذهب إلى Deployments
2. اضغط على "Redeploy" على آخر deployment

## 📞 الدعم

إذا استمرت المشاكل:
1. تحقق من Logs في Railway
2. تأكد من جميع الخطوات أعلاه
3. تحقق من أن الكود يعمل محلياً أولاً


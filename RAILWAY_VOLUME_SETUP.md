# إعداد Volume على Railway لحفظ البيانات

## المشكلة
البيانات (قاعدة البيانات والصور) تُحفظ في filesystem عادي و**تضيع عند إعادة البناء**!

## الحل: استخدام Railway Volume

### الخطوة 1: إنشاء Volume في Railway

1. في Railway Dashboard، اضغط **"New"**
2. اختر **"Volume"**
3. املأ البيانات:
   - **Name:** `ecommerce-data`
   - **Size:** 1GB (مجاني)
4. اضغط **"Create"**

### الخطوة 2: ربط Volume بالخدمة

1. اضغط على الخدمة (Service) الخاصة بك
2. اضغط **"Settings"** → **"Volumes"**
3. اضغط **"Add Volume"**
4. اختر Volume الذي أنشأته
5. **Mount Path:** `/data`
6. اضغط **"Add"**

### الخطوة 3: إضافة Environment Variable

1. في صفحة الخدمة، اضغط **"Variables"**
2. أضف متغير جديد:
   ```
   RAILWAY_VOLUME_MOUNT_PATH=/data
   ```
3. اضغط **"Add"**

---

## ✅ تم!

الآن:
- ✅ قاعدة البيانات تُحفظ في `/data/database.sqlite`
- ✅ الصور تُحفظ في `/data/uploads`
- ✅ البيانات **لن تضيع** عند إعادة البناء!

---

## ملاحظات

- Volume **يدوم** حتى بعد إعادة البناء
- البيانات تبقى آمنة
- يمكنك رفع/تنزيل البيانات من Railway

---

## التحقق

بعد إعادة البناء:
1. ارفع منتج جديد مع صورة
2. تحقق أن الصورة تظهر
3. أعد بناء المشروع
4. الصورة ما زالت موجودة! ✅

**بالتوفيق! 🚀**


# 🚀 دليل النشر على Railway - خطوة بخطوة

## ✅ التحقق من الجاهزية

قبل البدء، تأكد من:
- [x] `railway.json` موجود في الجذر
- [x] `package.json` موجود في الجذر
- [x] `client/package.json` موجود
- [x] لا يوجد `Dockerfile` في المشروع
- [x] المشروع موجود على GitHub

---

## 📋 خطوات النشر

### الخطوة 1: ارفع المشروع على GitHub

```bash
# تأكد أنك في مجلد المشروع
cd "C:\Users\asad2\OneDrive\سطح المكتب\website"

# أضف جميع الملفات
git add .

# Commit
git commit -m "Ready for Railway deployment"

# Push إلى GitHub
git push origin main
```

### الخطوة 2: أنشئ مشروع جديد على Railway

1. اذهب إلى [railway.app](https://railway.app)
2. سجل دخول بحساب GitHub
3. اضغط **"New Project"**
4. اختر **"Deploy from GitHub repo"**
5. اختر الـ repository الخاص بك

### الخطوة 3: إعدادات Railway (مهم جداً!)

في صفحة المشروع → **Settings**:

#### Build Settings:
- **Builder:** يجب أن يكون **`NIXPACKS`** (إذا كان `Docker`، غيّره!)
- **Build Command:** اتركه فارغاً (سيستخدم من `railway.json`)
- **Start Command:** اتركه فارغاً (سيستخدم من `railway.json`)

#### Environment Variables:
اضغط **"Variables"** → **"New Variable"**:

```
NODE_ENV = production
```

⚠️ **مهم:** 
- `PORT` يتم إضافته تلقائياً من Railway - لا تضيفه
- لا تضيف `JWT_SECRET` إلا إذا كان الكود يحتاجه

### الخطوة 4: انتظر البناء

- Railway سيقوم بـ:
  1. تثبيت dependencies للجذر (`npm install`)
  2. تثبيت dependencies للـ client (`cd client && npm install`)
  3. بناء تطبيق الـ client (`cd client && npm run build`)
  4. تشغيل السيرفر (`npm start`)

### الخطوة 5: احصل على الرابط

- بعد اكتمال البناء، اضغط **"Settings"**
- في قسم **"Domains"** اضغط **"Generate Domain"**
- أو أضف domain مخصص

---

## 🔧 إذا فشل البناء

### المشكلة: "Builder is Docker"

**الحل:**
1. اذهب إلى Settings
2. غيّر Builder من `Docker` إلى `NIXPACKS`
3. أعد النشر

### المشكلة: "can't cd to client"

**الحل:**
- تأكد أن مجلد `client/` موجود
- تأكد أن `railway.json` موجود
- تأكد أن لا يوجد `Dockerfile`

### المشكلة: "npm run install-all failed"

**الحل:**
- تحقق من Logs في Railway
- تأكد أن `package.json` موجود في الجذر
- تأكد أن `client/package.json` موجود

---

## 📊 مراقبة النشر

1. **Deployments Tab:** شاهد حالة البناء
2. **Logs Tab:** شاهد السجلات في الوقت الفعلي
3. **Metrics Tab:** شاهد استخدام الموارد

---

## ✅ بعد النشر الناجح

1. **اختبر الموقع:**
   - افتح الرابط
   - تأكد أن الصفحة الرئيسية تعمل
   - جرب البحث
   - جرب إضافة منتج للسلة

2. **إعدادات Admin:**
   - اذهب إلى `/admin/login`
   - اسم المستخدم: `web`
   - كلمة المرور: `web12345`
   - غيّر كلمة المرور بعد أول دخول!

3. **إضافة المنتجات:**
   - سجل دخول كـ admin
   - أضف منتجات
   - أضف صور
   - حدّث الإعدادات

---

## 🎯 الملفات المطلوبة

```
website/
├── railway.json          ✅ (مطلوب)
├── package.json          ✅ (مطلوب)
├── client/
│   └── package.json      ✅ (مطلوب)
├── server/
│   └── index.js          ✅ (مطلوب)
└── (لا Dockerfile)       ✅ (مهم!)
```

---

## 🚨 نصائح مهمة

1. **لا تضيف Dockerfile** - سيسبب مشاكل
2. **تأكد من Builder = NIXPACKS** في Settings
3. **أضف `NODE_ENV=production`** في Variables
4. **تحقق من Logs** إذا فشل البناء
5. **انتظر حتى يكتمل البناء** قبل الاختبار

---

## 📞 الدعم

إذا استمرت المشاكل:
1. تحقق من Logs في Railway
2. تأكد من جميع الخطوات أعلاه
3. تأكد من أن الكود يعمل محلياً أولاً

---

## 🎉 النتيجة

بعد اكتمال النشر، ستحصل على:
- ✅ رابط دائم للموقع
- ✅ HTTPS تلقائي
- ✅ تحديثات تلقائية عند push إلى GitHub
- ✅ Logs و Metrics

**الموقع جاهز للاستخدام!** 🚀


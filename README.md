# نظام متجر إلكتروني احترافي

نظام متجر إلكتروني احترافي قابل للبيع كقالب، مصمم ليكون سهل الاستخدام بدون الحاجة لخبرة تقنية.

## 🎯 المميزات

### المميزات الأساسية
- ✅ لوحة تحكم سهلة الاستخدام
- ✅ إضافة/تعديل/حذف المنتجات
- ✅ رفع الصور بطريقة Drag & Drop
- ✅ إدارة الأسعار والخصومات
- ✅ حساب نسبة الخصم تلقائياً
- ✅ تصميم Responsive (Mobile First)
- ✅ واجهة مستخدم احترافية
- ✅ إدارة العروض والخصومات
- ✅ نظام تخصيص كامل (Logo, ألوان, إعدادات)
- ✅ أزرار تواصل سريع (واتساب, مكالمة, إنستغرام)

### المميزات التقنية
- Frontend: React + Vite + Tailwind CSS
- Backend: Node.js + Express
- Database: SQLite (يمكن الترقية لـ MySQL)
- Authentication: JWT
- File Upload: Multer

## 📦 التثبيت

### المتطلبات
- Node.js 16+ 
- npm أو yarn

### خطوات التثبيت

1. **تثبيت الحزم:**
```bash
npm run install-all
```

2. **إعداد ملف البيئة:**
```bash
cp .env.example .env
```

3. **تشغيل المشروع:**
```bash
npm run dev
```

سيتم تشغيل:
- Backend على: http://localhost:5000
- Frontend على: http://localhost:3000

## 🔐 بيانات الدخول الافتراضية

- **اسم المستخدم:** `admin`
- **كلمة المرور:** `admin123`

⚠️ **مهم:** قم بتغيير كلمة المرور في الإنتاج!

## 📁 هيكل المشروع

```
.
├── client/                 # Frontend (React)
│   ├── src/
│   │   ├── components/    # المكونات
│   │   ├── pages/         # الصفحات
│   │   ├── context/        # Context API
│   │   └── utils/         # Utilities
│   └── package.json
├── server/                 # Backend (Node.js)
│   ├── routes/            # API Routes
│   ├── uploads/           # الصور المرفوعة
│   └── database.sqlite    # قاعدة البيانات
└── package.json
```

## 🚀 الاستخدام

### إضافة منتج جديد
1. سجل الدخول إلى لوحة التحكم: `/admin/login`
2. اضغط على "إضافة منتج جديد"
3. املأ البيانات:
   - اسم المنتج (عربي/إنجليزي)
   - الوصف
   - السعر الأصلي
   - السعر بعد الخصم (اختياري)
   - ارفع صورة المنتج
4. احفظ المنتج

### تخصيص المتجر
1. اذهب إلى "الإعدادات" في لوحة التحكم
2. قم بتخصيص:
   - اسم المتجر
   - الشعار
   - الألوان
   - معلومات التواصل
   - بانر العروض

## 🎨 التخصيص

### تغيير الألوان
يمكن تغيير الألوان من لوحة التحكم في قسم "الإعدادات" بدون الحاجة لتعديل الكود.

### إضافة ميزات جديدة
المشروع مبني بطريقة معيارية، يمكنك بسهولة:
- إضافة صفحات جديدة
- إضافة ميزات جديدة
- ربط نظام دفع
- إضافة نظام توصيل

## 📱 Responsive Design
التصميم متجاوب بالكامل ويعمل على:
- 📱 الهواتف المحمولة
- 📱 الأجهزة اللوحية
- 💻 أجهزة الكمبيوتر

## 🔒 الأمان
- JWT Authentication
- Password Hashing (bcrypt)
- File Upload Validation
- SQL Injection Protection

## 📝 API Endpoints

### Products
- `GET /api/products` - جلب جميع المنتجات
- `GET /api/products/:id` - جلب منتج محدد
- `POST /api/products` - إضافة منتج جديد
- `PUT /api/products/:id` - تحديث منتج
- `DELETE /api/products/:id` - حذف منتج

### Settings
- `GET /api/settings` - جلب الإعدادات
- `PUT /api/settings` - تحديث الإعدادات

### Auth
- `POST /api/auth/login` - تسجيل الدخول

## 🛠 البناء للإنتاج

```bash
# بناء Frontend
cd client
npm run build

# تشغيل الإنتاج
NODE_ENV=production npm start
```

## 📄 الترخيص
MIT License - قابل للبيع والاستخدام التجاري

## 🤝 الدعم
للدعم والاستفسارات، يرجى التواصل مع المطور.

---

**ملاحظة:** هذا النظام قابل للبيع لأكثر من زبون. كل زبون يمكنه استخدامه بشكل مستقل.


